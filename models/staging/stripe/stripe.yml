version: 2

sources:
  - name: stripe
    database: raw
    tables:
      - name: payment    

models:
    - name: stg_payments
      description: One row per payment id
      columns:
        - name: payment_id
          tests:
            - unique
            - not_null